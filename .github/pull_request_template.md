**What & why**
Short description of the change and the problem it solves.

**Checklist**
- [ ] `bun test`, `bun run lint`, `bun run typecheck`, `bun run build` all pass
- [ ] No new runtime dependencies (`dependencies` stays `{}`); `inject` stays a silent no-op on error; no network (see `CLAUDE.md`)
- [ ] New extraction filter? Added a row to `docs/HEURISTICS.md`
- [ ] One focused concern
